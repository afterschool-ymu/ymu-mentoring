/* =====================================================================
   THE ONE PLACE YOU EDIT.

   Paste your deployed Apps Script web app URL below — the one that ends
   in /exec — then commit. Both public links point at it.

   Why this file exists:
   Every time you create a NEW Apps Script deployment, Google gives you a
   NEW /exec URL. Without this indirection you would have to re-email 15
   mentors and every family. With it, you change one line here and every
   link that is already in someone's inbox keeps working.

   Deploy > Manage deployments > Edit (pencil) > New version  keeps the
   same URL. Deploy > New deployment  makes a new one. Prefer the former.
   ===================================================================== */

window.YMU = {
  // e.g. 'https://script.google.com/macros/s/AKfycb...../exec'
  webAppUrl: 'https://script.google.com/macros/s/AKfycbzjq85TQ74jziPDki_mqgN0-_8lbeYwlS6JjxyL9H_T6E8dBOxVhjYDGl5fzAHbn9oZ/exec',

  // Shown to anyone who gets stuck.
  coordinatorEmail: 'afterschool@ymu.org',
  programmeName: 'YMU Music Mentoring'
};
